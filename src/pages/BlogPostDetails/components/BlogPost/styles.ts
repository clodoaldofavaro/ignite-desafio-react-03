import styled from 'styled-components'

export const BlogPostBodyContainer = styled.section`
  padding: 40px 32px;
  position: relative;
  top: 416px;
  z-index: 2;
  max-width: 960px;
  left: 50%;
  transform: translateX(-50%);
  font-size: 16px;
`
